import 'package:flutter/material.dart';

class AppPaddings {
  // all
  static const g24 = EdgeInsets.all(24.0);
  static const g16 = EdgeInsets.all(16.0);

  // vertical
  static const gV16 = EdgeInsets.symmetric(vertical: 16);

  // horizontal
  static const gH16 = EdgeInsets.symmetric(horizontal: 16);

  // symmetric
  static const gH12V6 = EdgeInsets.symmetric(horizontal: 12, vertical: 6);
}
